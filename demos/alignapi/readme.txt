Demos for S-Match integration into AlignAPI. 

To run the demo:

1) Set alignapi.dir in the build.properties file to point to AlignAPI folder.
2) Run "ant" which will build the matcher
3) Edit bin/SMatchMatcher.cmd to set the variables correctly for AlignAPI folder and source and target ontologies
4) Run bin/SMatchMatcher.cmd